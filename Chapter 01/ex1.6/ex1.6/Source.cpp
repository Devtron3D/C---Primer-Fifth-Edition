// Exercise 1.6

// std::cout << "The sum of " << v1;
//           << " and " << v2;
//			 << " is " << v1 + v2 << std::endl;

//  The program is not legal, as the expression is eneded after ; 
//  so the last two lines of code will not be executed;
//	This will also not compile;
